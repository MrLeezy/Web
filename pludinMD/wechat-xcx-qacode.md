请先阅读并严格遵循 `docs/plugin-spec.md`。

你是在当前仓库内开发功能，不是新建独立项目。请直接在现有项目里实现，不要新开工程，不要引入新的前端框架、打包器或第二套服务。所有插件实现必须服从当前项目结构：前端文件放在 `plugins/`，页面入口为 `/plugins/<plugin-id>.html`，后端接口统一挂在 `/api/plugins/<plugin-id>/...`，并在 `server.js` 的 `DEFAULT_PLUGIN_CARDS` 中注册卡片。所有运行数据放在 `data/` 或 `private/` 下的插件专属目录中，错误统一返回 JSON。

本次任务类型：
- 新增插件

插件信息：
- 插件名称：小程序二维码生成器
- 插件 ID：wechat-xcx-qacode

任务目标：
- 支持上传 `csv`、`txt`、`xlsx` 文件，并识别文件内容与记录数量
- 支持提供上传模板，模板字段至少包含：`title`、`path`
- 识别每条记录中的小程序标题和小程序路径
- 自动唤起浏览器并打开 `https://mp.weixin.qq.com/`
- 等待我人工扫码登录成功后，再继续后续流程
- 自动进入对应模块，为每条记录生成小程序二维码
- 下载生成后的二维码图片，并以对应 `title` 作为文件名保存
- 所有二维码保存到插件专属输出目录，并按本次任务日期创建新文件夹
- 全部完成后结束任务，本次任务文件夹不再追加其他图片

功能要求：
- 页面需要支持上传文件，并展示识别结果与总数量
- 页面需要支持下载/查看模板说明
- 浏览器自动化过程必须可见，不能静默隐藏关键步骤，默认使用非 headless 模式
- 登录必须由人工扫码完成，不能要求输入账号密码，不能把登录态或敏感信息提交到仓库
- 需要循环处理上传文件中的每一条记录
- 每条记录处理流程如下：
  1. 打开微信公众平台页面并等待人工扫码登录
  2. 找到“小程序码”按钮后进入生成流程
  3. 在输入框中填写小程序路径
  4. 点击“确定”生成二维码
  5. 获取生成后的二维码图片并保存到本地
  6. 点击“完成”结束当前条目的生成
  7. 继续下一条记录，直到全部完成
- 输出目录规则建议为：`data/wechat-xcx-qacode/outputs/YYYY-MM-DD/`
- 图片文件名使用对应记录的 `title`，如遇重名需自动处理，避免覆盖
- 如果单条记录生成失败，需要记录失败原因，并继续处理后续记录；全部任务结束后返回成功数、失败数和失败明细

页面元素定位要求：
- 可参考以下页面元素特征，但不要只依赖脆弱的 class 名
- 优先根据可见文本、按钮文案、稳定 DOM 特征进行定位
- 可参考的元素特征包括：
  - “小程序码”按钮
  - 路径输入框：`<input type="text" class="weui-desktop-form__input">`
  - “确定”按钮
  - 结果图片区域：`<div class="image-wrp"><img src=...></div>`
  - “完成”按钮

边界要求：
- 不要修改其他插件的行为
- 不要重构整个插件系统
- 不要影响现有项目其他功能
- 如果实现方式和当前仓库结构冲突，优先服从当前仓库结构
- 不要把功能做成独立项目
- 不要把微信账号信息、cookie、缓存等敏感内容提交进仓库

交付要求：
- 直接给出代码修改
- 列出修改文件
- 列出接口变更
- 说明数据目录
- 给出手动验证步骤
- 给出已知限制和风险
- 交付格式请遵循 `docs/plugin-delivery-checklist.md`
