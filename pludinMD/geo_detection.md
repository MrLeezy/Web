请先阅读并严格遵循 `docs/plugin-spec.md`。

你是在当前仓库内开发功能，不是新建独立项目。请直接在现有项目里实现，不要新开工程，不要引入新的前端框架、打包器或第二套服务。所有插件实现必须服从当前项目结构：前端文件放在 `plugins/`，页面入口为 `/plugins/<plugin-id>.html`，后端接口统一挂在 `/api/plugins/<plugin-id>/...`，并在 `server.js` 的 `DEFAULT_PLUGIN_CARDS` 中注册卡片。所有运行数据放在 `data/` 或 `private/` 下的插件专属目录中，错误统一返回 JSON。

本次任务类型：
- 新增插件

插件信息：
- 插件名称：GEO长尾词监控
- 插件 ID：geo-detection

任务目标：
- 支持访问各国内智能Chat平台，如：
（1、豆包：[开启思考](https://www.doubao.com/chat/)
（2、Deepseek：[开启深度思考，开始智能搜索](https://chat.deepseek.com/)
（3、千问：[开启深度思考](https://www.qianwen.com/)
（4、元宝：[开启深度思考](https://yuanbao.tencent.com/chat/naQivTmsDa) 
- 支持品牌词录入，用于后续各智能Chat平台的核对
- 最终反馈我的长尾词在各Chat平台是否进行命中，以及输出报告。
- 用最简单的便捷的方式完成整体操作；
- 有可能各Chat平台需要登录，想办法跳过
- 整体流程需要完成，不要出现各种BUG

功能要求：
- 有前端页面可以选择标签，输入一段长尾词；
- 选择智能Chat平台，四个默认全选，也可以不选择某一个；
- 按照顺序，自动打开各Chat平台，按照需求完成配置，然后自己找到输入框，输入长尾词
- 各平台会回复对应的内容，然后查看回复结果是否包含品牌词
- 然后打开各平台的参考资料，查询参考资料是否命中品牌词；
- 最终输出报告，这个报告是累计的，同时间，同长尾词，同平台进行数据更新，否是新增行数。
（1、时间：当前年月日，不用到时分秒
（2、长尾词：输入的长尾词是什么
（3、哪个Chat平台
（4、内容区域是否有匹配
（5、内容区域是否有匹配哪个品牌词，如果多个用|进行区分
（6、参考资料是否有匹配
（7、参考资料是否有匹配哪个品牌词，如果多个用|进行区分

边界要求：
- 不要修改其他插件的行为
- 不要重构整个插件系统
- 不要影响现有项目其他功能
- 如果实现方式和当前仓库结构冲突，优先服从当前仓库结构
- 不要把功能做成独立项目
- 不要把微信账号信息、cookie、缓存等敏感内容提交进仓库

交付要求：
- 直接给出代码修改
- 列出修改文件
- 列出接口变更
- 说明数据目录
- 给出手动验证步骤
- 给出已知限制和风险
- 交付格式请遵循 `docs/plugin-delivery-checklist.md`
