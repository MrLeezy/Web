请先阅读并严格遵循 `docs/plugin-spec.md`。

你是在当前仓库内开发功能，不是新建独立项目。请直接在现有项目里实现，不要新开工程，不要引入新的前端框架、打包器或第二套服务。所有插件实现必须服从当前项目结构：前端文件放在 `plugins/`，页面入口为 `/plugins/<plugin-id>.html`，后端接口统一挂在 `/api/plugins/<plugin-id>/...`，并在 `server.js` 的 `DEFAULT_PLUGIN_CARDS` 中注册卡片。所有运行数据放在 `data/` 或 `private/` 下的插件专属目录中，错误统一返回 JSON。

本次任务类型：
- 新增插件

插件信息：
- 插件名称：comsumer 数据生成器
- 插件 ID：comsumer-data

任务目标：
- 按照桌面的日历生成数据报告，可以选择季度和周期。
- 主要完成comsumer数据底层表添加数据。
- 合并的数据包括线下数据上传，数据库读取
- 线下数据有一些不规范的数据需要清洗
- 所有数据合并完成需要进行部分数据拆解到另一个表里，并自动发送邮件。
- 数据表合并完成，提炼里面的数据到数据分析表里面

目标数据表：
- /Users/lizhongyu/程序/团队协作系统/data/comsumer/【FY27Q1 W8】User Rawdata.xlsx 重点是需要了解表里面每一个sheet，以及对应的字段，以及都是干什么，互相关联是什么。
- 每次数据添加出现一个新的表，【】里面的日期按照季度和Week展示
- 数据透视表可以不处理，主要根据后面的表进行的透视
- 唯一渠道数据分布，主要是渠道表，G - K列是标准的渠道，标准渠道映射表
- UTM 这个表包含后面所有的渠道来源，最终根据各种合并变成标准渠道，相当于统一映射表
- UserRawdata 后面的一些数据读取过来的数据
- DemandRawdata 根据UserRawdata获取一些数据
- ProductRawdata 所有产品数据，部分数据会通过数据库进行读取
- GIO 线下数据表
- Unionid 数据库读取
- User 数据库读取
- Q-Chat 线下表提供
- Funnel线下表
- Sunny Offline 线下表
- Order数据库读取
- 机器人 线下表
- B2B 线下表
- 领取积分 数据库读取
- 领取优惠券 数据库读取
- 订阅模版消息 数据库读取
- 0元试用申请 数据库读取
- 参与抽奖 数据库读取
- 参与周年庆活动 数据库读取
- 频道数据情况 数据库读取
- Dictionary 字典表，主要维护一些之前表

功能要求：
- 页面根据首页的标准日历进行选择季度月度周度一些字段
- 如果当前日期已经有创建文件，补充数据进行更新，如果选择的日期没有制作过对应报告，创建新的文件但保留当季度所有的历史数据
- 需要数据库连接的自动进行抓取
- 线下数据上传的，我可以进行分批上传
- 里面有一些映射关系与字典表这样的内容，能自动最好，确认不好的可以让我来简单完成操作
- 当这个底层表完成后，有两个按钮：一个是 生成周报，一个是生成Demand
- 生成周报是从这个底层数据取一些数据放到另一个表里（可以后面在沟通，因为这个项目比较复杂，后面在聊）
- 生成Demand从这个底层数据取一些数据放到另一个表里，然后把表自动发邮件给一些邮箱里（可以后面在沟通，因为这个项目比较复杂，后面在聊）

边界要求：
- 不要修改其他插件的行为
- 不要重构整个插件系统
- 不要影响现有项目其他功能
- 如果实现方式和当前仓库结构冲突，优先服从当前仓库结构
- 不要把功能做成独立项目
- 不要把微信账号信息、cookie、缓存等敏感内容提交进仓库

交付要求：
- 直接给出代码修改
- 列出修改文件
- 列出接口变更
- 说明数据目录
- 给出手动验证步骤
- 给出已知限制和风险
- 交付格式请遵循 `docs/plugin-delivery-checklist.md`
